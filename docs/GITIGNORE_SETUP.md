# GITIGNORE 管理ガイド

このドキュメントは、チームで利用する `.gitignore` ファイルの管理方法をまとめたものです。
開発言語ごとのテンプレートを Git 管理し、プロジェクト開始時に組み合わせて利用できるようにします。

---

## 📂 ファイル構成

- `gitignore-templates/`
  - 言語ごとの `.gitignore` テンプレートを保存
  - 例:
    - `python.gitignore`
    - `ruby.gitignore`
    - `go.gitignore`
    - `java.gitignore`
    - `node.gitignore`
  - 共通ルールは `common.gitignore` にまとめる（OS 固有ファイルや一般的な除外対象）

- `docs/GITIGNORE_SETUP.md`
  - 本ドキュメント（利用方法や標準化ルールを記載）

---

## 🛠 プロジェクトでの利用方法

### 1. 言語別テンプレートの選択
プロジェクト開始時に、必要な言語のテンプレートを選びます。

例: Python プロジェクトの場合
```bash
cat gitignore-templates/common.gitignore gitignore-templates/python.gitignore > .gitignore
